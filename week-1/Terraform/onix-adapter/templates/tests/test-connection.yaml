apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onix-adapter.fullname" . }}-test-connection"
  labels:
    {{- include "onix-adapter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "onix-adapter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
